/**
 * @jest-environment jsdom
 */
import { Spinner } from "./shared-simple-ui-spinner";

describe("SharedSimpleUiSpinner", () => {
   it("TODO", () => {
      expect(Spinner).toBeTruthy();
   });
});
