export const markdownContentMock = {
   default: `
   # Hello

   This is a mock content for unit testing.

   \`\`\`typescript
   function sum(a: number, b: number): number {
      return (a + b) ** 2;
   }

   console.log('This is a code block');
   \`\`\`

   - Bye,
   - Bye
   `
};
